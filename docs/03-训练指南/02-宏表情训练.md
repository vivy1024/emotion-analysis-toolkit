# 宏表情训练指南

## 概述

宏表情识别支持 FER2013 等公开数据集，训练 7 类基本情绪分类模型。

## 训练命令

```bash
python -m macro_expression.multi_dataset_train --dataset fer2013
```

## 关键模块

- `multi_dataset_train.py` — 多数据集训练入口
- `emotion_model.py` — 模型推理和单图预测

## 输出

训练好的模型保存为 `macro.pt`，用于实时检测系统的宏表情引擎。
